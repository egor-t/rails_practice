<%= form_with(model: railsway_station, local: true) do |form| %>
  <% if railsway_station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(railsway_station.errors.count, "error") %> prohibited this railsway_station from being saved:</h2>

      <ul>
      <% railsway_station.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :railsway_station_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
