<h1><%= t('admin.trains.index.title')%></h1>

<table>
  <thead>
    <tr>
      <th><%= t('admin.trains.index.number')%></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @trains %>
  </tbody>
</table>

<br>

<%= link_to t('common.new'), new_admin_train_path %>