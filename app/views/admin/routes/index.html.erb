<h1><%=t('admin.routes.index.title') %></h1>

<table>
  <thead>
    <tr>
      <th><%=t('admin.routes.show.title') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @routes %>
  </tbody>
</table>


<%= link_to t('admin.routes.new.title'), new_admin_route_path %>